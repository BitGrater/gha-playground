name: Integrate

on:
  workflow_dispatch:
    inputs:
      comment-id:
        description: ID of the comment which triggered this workflow
        required: true
        type: string

jobs:
  integrate:
    name: Integrate
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: peter-evans/create-or-update-comment@v2
        with:
          body: |
            Starting integrate workflow :rocket:
      - uses: actions/checkout@v3
      - run: echo "Hello from Integrate!"